---
import MainNav from "./main-nav.astro";

const { items } = Astro.props;
---

<header
  class="container flex items-center h-18 z-50 sticky top-0 bg-background"
>
  <nav class="flex items-center justify-between w-full">
    <MainNav items={items} />

    <slot />
  </nav>
</header>
